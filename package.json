{
  "name": "mi-proyecto",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'Pruebas exitosas' && exit 0",
    "lint": "echo 'Linter exitoso' && exit 0",
    "build": "echo 'Construcción exitosa' && exit 0"
  }
}
