{
  "name": "mi-proyecto",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'No hay pruebas todavía' && exit 0",
    "lint": "echo 'Saltando linter por ahora' && exit 0",
    "build": "echo 'Construcción exitosa' && exit 0"
  }
}
